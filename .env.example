PORT=51999
DEFAULT_SYNC_PORT=52000
VIDEO_STORAGE_ROOT=/video52000/videos
MAX_UPLOAD_MB=2048

# The first admin user is created only when data/db.json has no matching user.
# Change these before the first production start.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_DISPLAY_NAME=Admin
DEFAULT_ADMIN_PASSWORD=change-me-now
